name: CI

on: 
  pull_request:
  push:
    branches: [master]

jobs:
  sim:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Hello repo
        uses: actions/checkout@v2
        with:
          path: helloworld
          fetch-depth: 0

      - name: Run cmds in vm
        run: |
          uname -a
          pwd
          ls
