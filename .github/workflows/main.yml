name: CI

on: 
  pull_request:
  push:
    branches: [master]

jobs:
  sim:
    runs-on: ubuntu-latest
    steps:
      - name: Run in vm
        uses: actions/checkout@v1
      - name: Run cmds in container
        run: |
          uname -a
          pwd
