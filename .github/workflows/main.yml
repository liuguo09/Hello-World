name: CI

on: 
  pull_request:
  push:
    branches: [master]

jobs:
  sim:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          path: helloworld
          fetch-depth: 0
      - run: git fetch origin +refs/tags/*:refs/tags/*

      - name: Run cmds in vm
        run: |
          uname -a
          pwd
          ls
